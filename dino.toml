# Config file for Dinosaur Planet Recompilation.

[input]
entrypoint = 0x80000400
# Paths are relative to the location of this config file.
output_func_path = "RecompiledFuncs"
rom_file_path = "baserom.z64"
symbols_file_path = "dino.syms.toml"
relocatable_sections_path = "dino.dlls.txt"

[patches]
stubs = [

]

ignored = [
    # TODO: handling this function manually (these are all supposed to be one function)
    "func_8001B4F0", "func_8001B6F0", "func_8001BC00", "func_8001BCF0", "func_8001BF70"
]
